# taskflow-api

API desenvolvida na disciplina de Integração e Entrega Contínua.

## Como executar

```bash
pip install flask
python app.py
```

## Rotas

- GET `/` — Página inicial
- GET `/status` — Status da API
